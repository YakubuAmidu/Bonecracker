import { colors } from './Colors';
import { fontWeights, fontSizes, fonts } from './Fonts';
import { sizes } from './Sizes';
import { lineHeights, space } from './Spacing';

export const theme = {
  colors,
  fontSizes, 
  fontWeights,
  lineHeights,
  fonts,
  space,
  sizes,
};